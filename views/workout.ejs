<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Workout for <%=fitnessData.appName%></title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
</head>
<body>
  <h1>My workouts</h1>

  <div class="top-actions">
    <a href="/workouts/new">Add New Workout</a>
    <a href="/workouts/search">Search Workouts</a>
    <a href="/">Back to Home</a>
  </div>

  <% if (workout.length === 0){ %>
    <p>You haven't logged any workouts yet, using the 'Add New Workout' to add one!</p>
  <% } else{ %>

    <table>
      <caption>Workouts</caption>
      <thead>
        <tr>
          <th>Date</th>
          <!-- <th>Title</th>
          <th>Notes</th> -->
          <th>Details</th>
        </tr>
      </thead>

      <% workout.forEach(workout => {  %>
        <tbody>
          <tr>
            <td><%= workout.workout_date.toISOString().split("T")[0] %></td>
            <!-- <td><%= workout.title %></td> -->
            <!-- <td><%= workout.notes || '-' %></td> -->
            <td><a href="/workouts/<%= workout.id %>">View</a></td>
          </tr>
        </tbody>

      <% }); %>

    </table>
    <% } %>
  
</body>
</html>

